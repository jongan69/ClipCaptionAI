# Rotato templates

Create `<template-id>/scene.rotato` from Rotato and place `template.json` beside it. The JSON contract is shown in `examples/marketing/rotato-template.example.json`. ClipCaptionAI inspects the scene before every render and rejects changed or invalid slot mappings.
