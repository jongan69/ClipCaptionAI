# LOGO_SPEC — PrepAI

> Generated by `scripts/logo/build-spec.mjs` from `assets/logos/prepai/logo.svg`.
> This file is the complete, image-free description of the logo. Paste it into a
> text-only coding model to have it author Remotion animations for a logo it
> cannot see.

## 1. Canvas

| Property | Value |
|---|---|
| viewBox | `0 0 1080 720` |
| Width x Height | 1080 x 720 |
| Origin | 0, 0 |
| Layer count | 3 |

## 2. Layer map

Every layer below is an independently animatable group. Bounding boxes are in
viewBox units. "Pivot" is the box centre — use it as `transform-origin` unless
the animation calls for something else.

| Layer id | Role | Bounding box | Pivot | Paint |
|---|---|---|---|---|
| `background` | backdrop | x 0 y 0 w 1080 h 720 | 540, 360 | — |
| `mark` | letterform | x 9 y 33 w 229 h 193 | 124, 130 | — |
| `wordmark` | text | x 381.6 y 569 w 316.8 h 92.2 | 540, 615 | #ffffff |

## 3. Gradients

- `#brandGradient` (linear): 0% → `#11518F`, 100% → `#08B8AE`
- `#bgGlow` (radial): 0% → `#142240`, 100% → `#0a1628`

## 4. Layer geometry (full path data)

### `background`
- **Role:** backdrop
- **Bounding box:** x=0, y=0, w=1080, h=720 — occupies 100.0% of canvas width, 100.0% of height
- **Transform origin / pivot:** `540px 360px`
- **Data:** `layer=background`, `role=backdrop`
```svg
<g id="background" data-layer="background" data-role="backdrop">
    <rect x="0" y="0" width="1080" height="720" fill="url(#bgGlow)"/>
  </g>
```
### `mark`
- **Role:** letterform
- **Bounding box:** x=9, y=33, w=229, h=193 — occupies 21.2% of canvas width, 26.8% of height
- **Transform origin / pivot:** `124px 130px`
- **Addressable children:** `heart-path`, `bar-short`, `bar-tall`, `pulse-path`
- **Data:** `layer=mark`, `role=letterform`
```svg
<g id="mark" data-layer="mark" data-role="letterform"
     transform="translate(231, -5) scale(2.5)">


    <path id="heart-path"
          fill="none" stroke="url(#brandGradient)" stroke-width="11"
          stroke-linecap="round" stroke-linejoin="round"
          d="M128 226 C111 209 52 161 34 113 C18 71 43 36 79 34 C101 33 117 47 128 64 C139 47 155 33 177 34 C213 36 238 71 222 113 C204 161 145 209 128 226"/>


    <rect id="bar-short" x="9" y="106" width="13" height="45" rx="4"
          fill="url(#brandGradient)" stroke="none"/>
    <rect id="bar-tall"  x="28" y="95" width="16" height="67" rx="4"
          fill="url(#brandGradient)" stroke="none"/>


    <path id="pulse-path"
          fill="none" stroke="url(#brandGradient)" stroke-width="11"
          stroke-linecap="round" stroke-linejoin="round"
          d="M44 128 H84 L93 128 L101 116 L111 143 L123 83 L136 169 L148 118 L158 128 H209"/>
  </g>
```
### `wordmark`
- **Role:** text
- **Bounding box:** x=381.6, y=569, w=316.8, h=92.2 — occupies 29.3% of canvas width, 12.8% of height
- **Transform origin / pivot:** `540px 615px`
- **Fill:** `#ffffff`
- **Data:** `layer=wordmark`, `role=text`, `text=prepai`, `center-x=540`, `baseline-y=640`, `font-size=96`, `fill=#ffffff`
```svg
<g id="wordmark" data-layer="wordmark" data-role="text"
     data-text="prepai" data-center-x="540" data-baseline-y="640"
     data-font-size="96" data-fill="#ffffff">
    <text x="540" y="640"
          fill="#ffffff"
          font-family="Inter, -apple-system, sans-serif"
          font-size="96"
          font-weight="300"
          letter-spacing="-3"
          text-anchor="middle">prepai</text>
  </g>
```

## 5. Brand

```json
{
  "name": "PrepAI",
  "slug": "prepai",
  "wordmark": {
    "text": "prepai",
    "accentText": "prepai",
    "accentSplitIndex": 0
  },
  "palette": {
    "background": "#0a1628",
    "backgroundGlow": "#142240",
    "primary": "#11518F",
    "primaryDark": "#0d4270",
    "accent": "#08B8AE",
    "accentLight": "#0dd4c8",
    "surface": "#ffffff",
    "ink": "#10263B",
    "lightBg": "#F6F6F8"
  },
  "fonts": {
    "wordmark": "Inter, -apple-system, sans-serif",
    "weight": 300
  },
  "notes": "The `heart` is the brand mark — a heart shape representing wellness and health intelligence. The `bars` (two vertical rectangles on the left) suggest health monitoring/metrics. The `pulse` is an ECG-style zigzag line running through the heart, evoking vital signs and real-time health tracking. Animations should read as 'monitor → analyze → empower' — the heart is the container, the pulse is the data, and the bars are the quantified insight."
}
```

## 6. What you must produce

Generate **10 distinct Remotion logo-animation variants**. Each variant is a
single `.tsx` file in `src/logo/variants/` exporting a default React component.

### Hard contract

```tsx
import type {LogoVariantProps} from '../types';

export const meta = {
  id: 'kebab-case-id',        // must match the filename
  name: 'Human Readable Name',
  durationInFrames: 120,      // at 30fps
  description: 'One line on what the animation does.',
};

export default function Variant({logo, frame, fps, width, height}: LogoVariantProps) {
  // ...
}
```

### Rules

1. **Never hardcode path data.** Read every path, colour, and box from the `logo`
   prop. The same variant must work for any brand that ships a spec. Access
   layers as `logo.layer('l-mark')`, `logo.layer('brackets')`, etc.
2. Import only from `remotion` and `react`. No new dependencies, no external
   assets, no network calls.
3. Animate with `interpolate`, `spring`, and `useCurrentFrame`. Never use
   CSS `@keyframes`, `transition`, or `setTimeout`.
4. Everything must be **deterministic** — identical output for a given frame.
   If you need randomness, use `random()` from `remotion` with a fixed seed.
5. Wrap SVG content in `<svg viewBox="0 0 1080 720">` and let the harness scale it.
6. Assume the layer may be missing: `logo.layer(id)` returns `null` if a brand
   lacks it. Degrade gracefully rather than throwing.
7. Land on the **static, correct logo** by the final frame. An animation that ends
   mid-transform is a bug.
8. Keep each variant under ~120 lines.

### Variant directions to cover

Aim for range — do not produce 10 easings of the same idea.

1. **Scan-in** — brackets draw on via `stroke-dasharray`, then the mark fades up inside them.
2. **Assemble** — layers fly in from offscreen edges and settle with a spring.
3. **Card deal** — the card layer slides and rotates into place like a dealt card.
4. **Mask wipe** — a diagonal gradient wipe reveals the mark left to right.
5. **Letter cascade** — wordmark letters rise and fade in one at a time.
6. **Depth parallax** — layers translate at different Z-rates on a slow push-in.
7. **Liquid morph** — the mark scales from a dot with an overshoot spring.
8. **Glitch resolve** — RGB-split offsets converge onto the clean logo.
9. **Bracket pulse** — logo settles first, brackets pulse as a shutter/capture beat.
10. **Gradient sweep** — a highlight travels across the mark after it lands.

Number the files `01-scan-in.tsx`, `02-assemble.tsx`, and so on. After writing them,
register each in `src/logo/registry.ts`.
