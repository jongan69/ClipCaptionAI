# LOGO_SPEC — OpenDating

> Generated by `scripts/logo/build-spec.mjs` from `assets/logos/opendating/logo.svg`.
> This file is the complete, image-free description of the logo. Paste it into a
> text-only coding model to have it author Remotion animations for a logo it
> cannot see.

## 1. Canvas

| Property | Value |
|---|---|
| viewBox | `0 0 1080 1080` |
| Width x Height | 1080 x 1080 |
| Origin | 0, 0 |
| Layer count | 5 |

## 2. Layer map

Every layer below is an independently animatable group. Bounding boxes are in
viewBox units. "Pivot" is the box centre — use it as `transform-origin` unless
the animation calls for something else.

| Layer id | Role | Bounding box | Pivot | Paint |
|---|---|---|---|---|
| `background` | backdrop | x 0 y 0 w 1080 h 1080 | 540, 540 | — |
| `mark-left` | symbol-part | x 250 y 265 w 350 h 350 | 425, 440 | none |
| `mark-right` | symbol-part | x 480 y 265 w 350 h 350 | 655, 440 | none |
| `connector` | connector | x 830 y 180 w 0 h 260 | 830, 310 | none |
| `wordmark` | text | x 298 y 744.9 w 484 h 84.5 | 540, 787 | #E8735A |

## 3. Gradients

_None._

## 4. Layer geometry (full path data)

### `background`
- **Role:** backdrop
- **Bounding box:** x=0, y=0, w=1080, h=1080 — occupies 100.0% of canvas width, 100.0% of height
- **Transform origin / pivot:** `540px 540px`
- **Data:** `layer=background`, `role=backdrop`
```svg
<g id="background" data-layer="background" data-role="backdrop">
    <rect x="0" y="0" width="1080" height="1080" fill="#141413"/>
  </g>
```
### `mark-left`
- **Role:** symbol-part
- **Bounding box:** x=250, y=265, w=350, h=350 — occupies 32.4% of canvas width, 32.4% of height
- **Transform origin / pivot:** `425px 440px`
- **Fill:** `none`
- **Stroke:** `#E8735A` at width `112`
- **Data:** `layer=mark-left`, `role=symbol-part`
```svg
<g id="mark-left" data-layer="mark-left" data-role="symbol-part"
     fill="none" stroke="#E8735A" stroke-width="112"
     stroke-linecap="round" stroke-linejoin="round">
    <circle cx="425" cy="440" r="175"/>
  </g>
```
### `mark-right`
- **Role:** symbol-part
- **Bounding box:** x=480, y=265, w=350, h=350 — occupies 32.4% of canvas width, 32.4% of height
- **Transform origin / pivot:** `655px 440px`
- **Fill:** `none`
- **Stroke:** `#E8735A` at width `112`
- **Data:** `layer=mark-right`, `role=symbol-part`
```svg
<g id="mark-right" data-layer="mark-right" data-role="symbol-part"
     fill="none" stroke="#E8735A" stroke-width="112"
     stroke-linecap="round" stroke-linejoin="round">
    <circle cx="655" cy="440" r="175"/>
  </g>
```
### `connector`
- **Role:** connector
- **Bounding box:** x=830, y=180, w=0, h=260 — occupies 0.0% of canvas width, 24.1% of height
- **Transform origin / pivot:** `830px 310px`
- **Fill:** `none`
- **Stroke:** `#E8735A` at width `112`
- **Data:** `layer=connector`, `role=connector`
```svg
<g id="connector" data-layer="connector" data-role="connector"
     fill="none" stroke="#E8735A" stroke-width="112"
     stroke-linecap="round">
    <path d="M830 180 L830 440"/>
  </g>
```
### `wordmark`
- **Role:** text
- **Bounding box:** x=298, y=744.9, w=484, h=84.5 — occupies 44.8% of canvas width, 7.8% of height
- **Transform origin / pivot:** `540px 787px`
- **Fill:** `#E8735A`
- **Addressable children:** `wordmark-primary`
- **Data:** `layer=wordmark`, `role=text`, `text=OpenDating`, `accent-text=`, `accent-split=0`, `primary-fill=#E8735A`, `accent-fill=#F8D5CD`, `font-family=-apple-system,BlinkMacSystemFont,'SF Pro Display',system-ui,sans-serif`, `font-size=88`, `font-weight=600`, `center-x=540`, `baseline-y=810`
```svg
<g id="wordmark" data-layer="wordmark" data-role="text"
     data-text="OpenDating" data-accent-text="" data-accent-split="0"
     data-primary-fill="#E8735A" data-accent-fill="#F8D5CD"
     data-font-family="-apple-system,BlinkMacSystemFont,'SF Pro Display',system-ui,sans-serif"
     data-font-size="88" data-font-weight="600"
     data-center-x="540" data-baseline-y="810"
     font-family="-apple-system,BlinkMacSystemFont,'SF Pro Display',system-ui,sans-serif"
     font-size="88" font-weight="600" letter-spacing="-3">
    <text id="wordmark-primary" x="540" y="810" text-anchor="middle"
          fill="#E8735A">OpenDating</text>
  </g>
```

## 5. Brand

```json
{
  "name": "OpenDating",
  "slug": "opendating",
  "wordmark": {
    "text": "OpenDating",
    "accentText": "",
    "accentSplitIndex": 0
  },
  "palette": {
    "background": "#141413",
    "backgroundLight": "#FAF9F7",
    "primary": "#E8735A",
    "primaryLight": "#FDF0ED",
    "primaryMuted": "#F8D5CD",
    "textPrimary": "#F5F4F2",
    "textSecondary": "#9B9B98",
    "accent": "#F8D5CD",
    "surface": "#1C1C1B"
  },
  "fonts": {
    "wordmark": "-apple-system, BlinkMacSystemFont, 'SF Pro Display', system-ui, sans-serif",
    "weight": 600
  },
  "notes": "Two overlapping circles represent people connecting. The vertical stem on the right represents growth, connection, or 'upright' dating. The overlapping area is the shared connection space. Coral (#E8735A) is the primary brand color — warm, human, approachable. Animations should feel warm, organic, and convey connection."
}
```

## 6. What you must produce

Generate **10 distinct Remotion logo-animation variants**. Each variant is a
single `.tsx` file in `src/logo/variants/` exporting a default React component.

### Hard contract

```tsx
import type {LogoVariantProps} from '../types';

export const meta = {
  id: 'kebab-case-id',        // must match the filename
  name: 'Human Readable Name',
  durationInFrames: 120,      // at 30fps
  description: 'One line on what the animation does.',
};

export default function Variant({logo, frame, fps, width, height}: LogoVariantProps) {
  // ...
}
```

### Rules

1. **Never hardcode path data.** Read every path, colour, and box from the `logo`
   prop. The same variant must work for any brand that ships a spec. Access
   layers as `logo.layer('l-mark')`, `logo.layer('brackets')`, etc.
2. Import only from `remotion` and `react`. No new dependencies, no external
   assets, no network calls.
3. Animate with `interpolate`, `spring`, and `useCurrentFrame`. Never use
   CSS `@keyframes`, `transition`, or `setTimeout`.
4. Everything must be **deterministic** — identical output for a given frame.
   If you need randomness, use `random()` from `remotion` with a fixed seed.
5. Wrap SVG content in `<svg viewBox="0 0 1080 1080">` and let the harness scale it.
6. Assume the layer may be missing: `logo.layer(id)` returns `null` if a brand
   lacks it. Degrade gracefully rather than throwing.
7. Land on the **static, correct logo** by the final frame. An animation that ends
   mid-transform is a bug.
8. Keep each variant under ~120 lines.

### Variant directions to cover

Aim for range — do not produce 10 easings of the same idea.

1. **Scan-in** — brackets draw on via `stroke-dasharray`, then the mark fades up inside them.
2. **Assemble** — layers fly in from offscreen edges and settle with a spring.
3. **Card deal** — the card layer slides and rotates into place like a dealt card.
4. **Mask wipe** — a diagonal gradient wipe reveals the mark left to right.
5. **Letter cascade** — wordmark letters rise and fade in one at a time.
6. **Depth parallax** — layers translate at different Z-rates on a slow push-in.
7. **Liquid morph** — the mark scales from a dot with an overshoot spring.
8. **Glitch resolve** — RGB-split offsets converge onto the clean logo.
9. **Bracket pulse** — logo settles first, brackets pulse as a shutter/capture beat.
10. **Gradient sweep** — a highlight travels across the mark after it lands.

Number the files `01-scan-in.tsx`, `02-assemble.tsx`, and so on. After writing them,
register each in `src/logo/registry.ts`.
